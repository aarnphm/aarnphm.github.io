/* W3C EBNF Grammar for Arithmetic Expressions */
/* For use with RR Railroad Diagram Generator: http://bottlecaps.de/rr/ui */

expression ::= ( '+' | '-' )? term ( ( '+' | '-' ) term )*

term ::= factor ( ( '*' | '/' ) factor )*

factor ::= number | identifier | '(' expression ')'

number ::= [0-9]+

identifier ::= [a-zA-Z_] [a-zA-Z0-9_]*
